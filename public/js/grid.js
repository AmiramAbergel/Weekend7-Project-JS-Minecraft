/**
 * @description Create new world
 * @param {number}
 */
export const drawGrid = (arr2D) => {
    for (let i = 0; i < arr2D.length; i++) {
        for (let j = 0; j < arr2D[i].length; j++) {}
    }
};
